# System
.DS_Store
.checkov.yaml
EXPLORATION_SUMMARY.txt
.augment/
.claude/
skills/

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
.terraform.lock.hcl
*.tfvars
!common.auto.tfvars
*.tfplan
tfplan
backend.tf
backend.hcl
# Track canonical backend definitions for Wiz IaC mapping
!infrastructure/backends/backend.tf
!infrastructure/demo/backend.tf
!infrastructure/demo-nowiz/backend.tf
# Auto-generated by `mise run bootstrap-branch`, but tracked to keep
# backend metadata aligned for Wiz IaC code-to-cloud mapping.
!infrastructure/backend-config.json
# Note: iac_config.wiz is NOT ignored - it must be committed for Wiz Code-to-Cloud mapping
# If you change AWS account/region, update the committed iac_config.wiz files.

# Mise/fnox (local config)
mise.local.toml
fnox.toml
kubeconfig

# Secrets & Sensitive Files
*.secrets.json
secrets.env
*.pem
*.key
*.p12
*.pfx

# Node.js
node_modules/
npm-debug.log
yarn-error.log
.next/
out/
build/
dist/

# Next.js
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Editor
.vscode/
.idea/
*.swp

# wizcli
wizcli

# Agent CLIs
GEMINI.md
AGENTS.md
CLAUDE.md

# Binary/Archive files (should not be in repo)
*.zip
*.tar.gz
*.pdf

# Legacy paths (no longer exist, kept for safety)
infra/
